import type { NextPage } from 'next';
import UpdateRequest from '../../components/UpdateRequest'; 

const Home: NextPage = () => {
  return (
   
    <UpdateRequest />
    
  );
};

export default Home;